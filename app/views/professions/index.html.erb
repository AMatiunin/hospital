<h1>Please select category of doctor:</h1>
<table>
  <thead>
  <tr>
    <th>Category</th>
  </tr>
  </thead>

  <tbody>

  <% @professions.each do |profession| %>
    <tr>
      <td><%= link_to profession.name, profession %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<% if current_user.is_a?(User) && current_user.admin? %>
  <%= link_to "Add Profession", new_profession_path%>
<% end %>